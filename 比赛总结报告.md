# 基于参考轨迹与强化学习的电动车风格化节能控制方案总结报告

## 1. 项目概述

本项目面向电动车节能控制任务，构建了一个基于参考驾驶轨迹的强化学习优化框架。系统以参考驾驶员轨迹为基础，通过智能体输出残差扭矩，对车辆在同一路段上的能耗进行优化，同时兼顾速度跟踪、位移一致性和平顺性约束。

本方案的核心目标不是单纯“把车开慢来省电”，而是在**尽量保持参考驾驶行为和到达时效一致**的前提下，学习更加节能的控制策略。

当前系统已经形成三种风格化控制模式：

- `eco`：稳健节能型
- `normal`：均衡型
- `sport`：高性能节能型

三种风格均在统一道路条件下训练与评估，只在奖励参数、约束容差和终局权重上进行风格化区分。

---

## 2. 方法亮点与创新点

### 2.1 参考轨迹 + 残差控制

本方案没有直接让强化学习从零输出整车控制，而是采用“参考轨迹 + 残差控制”的结构：

- 先由参考驾驶员模型生成参考速度、参考扭矩和参考能耗轨迹
- 智能体只在参考扭矩附近进行小范围优化
- 通过物理动力学模型计算真实车辆响应与能耗

这种结构兼顾了：

- 可解释性
- 训练稳定性
- 工程可落地性

### 2.2 奖励与约束双通道优化

本系统不是简单依靠一个 reward 学习，而是同时引入了：

- 主奖励：节能与跟踪目标
- 多约束成本：速度、距离、平顺性、投影误差、窗口能耗
- 拉格朗日乘子：对约束违规程度进行自适应调节

因此模型不仅在“追求更高节能”，也会自动避免：

- 跟踪误差过大
- 局部窗口能耗失真
- 过度抖动

### 2.3 风格化控制策略

相较于只训练一个统一策略，本项目进一步设计了风格化配置：

- `eco` 更强调稳定与可信节能
- `normal` 更强调节能与驾驶品质平衡
- `sport` 更强调高性能条件下的节能潜力

这使得系统能够针对不同应用场景输出不同取向的控制器，提升了方案的实用性与展示价值。

### 2.4 多指标联合评估，避免“虚假节能”

本项目同时评估：

- 真实节能率
- 训练口径节能率
- 等时节能率
- 滑动窗口节能率
- 工作点分布

这样可以区分：

- 真正通过高效率工作点获得的节能
- 通过速度偏慢或时间拉长造成的“虚假节能”

这对比赛展示尤其重要，因为它提升了结果的可信度。

---

## 3. 实验设置

### 3.1 道路与评估设置

系统采用统一 richer road 进行训练和测试，路况中包含：

- 坡度变化
- 曲线路段
- 城区低速段
- 近停车/再起步段

评估分为两类：

- `eval`：标准测试路况
- `stress`：压力测试路况，难度更高，用于检验泛化能力

### 3.2 模型输出

当前主线版本中，智能体输出的是**残差扭矩动作**，通过车辆动力学模型转换为实际车辆响应。

### 3.3 结果目录

当前主结果保存在：

- `results/eco/seed_42`
- `results/normal/seed_42`
- `results/sport/seed_42`

总汇总文件：

- `results/experiment_summary.json`

---

## 4. 当前结果总览

### 4.1 关键指标汇总

| 风格 | Eval真实节能率 | Eval速度MAE | Eval跟踪 | Stress真实节能率 | Stress跟踪 |
|---|---:|---:|---|---:|---|
| eco | +0.58% | 0.088 m/s | true | +11.41% | true |
| normal | +3.24% | 0.158 m/s | true | +0.79% | false |
| sport | +26.16% | 0.135 m/s | true | +31.60% | true |

数据来源：

- `results/eco/seed_42/metrics.json`
- `results/normal/seed_42/metrics.json`
- `results/sport/seed_42/metrics.json`

---

## 5. 各风格结果分析

### 5.1 Eco：稳健可信的节能基线

`eco` 模式是当前最稳健的一条线。

其特点是：

- 标准路况与压力路况下均实现正节能
- 跟踪约束在两类测试中都满足
- 结果较为保守，但可信度高

因此，`eco` 非常适合作为比赛中的“工程可落地基线”进行展示。

#### 图 1 Eco 主结果图

![Eco主结果图](results/eco/seed_42/plots/tracking_energy_eco.png)

#### 图 2 Eco 节能效果图

![Eco节能效果图](results/eco/seed_42/plots/energy_saving_effect_eco.png)

#### 图 3 Eco 工作点分布图

![Eco工作点分布图](results/eco/seed_42/plots/diagnostic_operating_points_eco.png)

---

### 5.2 Normal：均衡策略，具备较好的实际应用价值

`normal` 模式是最接近“默认驾驶策略”的方案。

在标准评测路况下，`normal` 已经实现：

- `3.24%` 的真实节能率
- `0.158 m/s` 的速度 MAE
- `tracking_ok = true`

说明该策略已经能在**不明显破坏驾驶品质**的情况下实现节能。

但其不足是：

- 在 `stress` 路况下虽然仍为正节能
- 但 `tracking_ok = false`

因此，`normal` 当前最适合作为“主线策略”，同时也表明其在复杂路况下仍有进一步增强泛化能力的空间。

#### 图 4 Normal 主结果图

![Normal主结果图](results/normal/seed_42/plots/tracking_energy_normal.png)

#### 图 5 Normal 实际扭矩偏差图

![Normal扭矩偏差图](results/normal/seed_42/plots/actual_torque_delta_normal.png)

#### 图 6 Normal 路段分段诊断图

![Normal路段分段诊断图](results/normal/seed_42/plots/diagnostic_route_segments_normal.png)

---

### 5.3 Sport：高性能上限策略

`sport` 模式在收紧容差和终局约束之后，已经从早期“虚假节能偏高”的状态，修正为当前可用版本。

当前 `sport` 的特点是：

- `eval` 与 `stress` 都满足跟踪要求
- 真实节能率非常高
- 表现出明显的高性能节能潜力

因此，`sport` 更适合作为比赛中的“性能上限展示版本”。

需要说明的是，`sport` 仍然是三种风格中最激进的一条线，因此更适合展示方法上限，而不是作为最保守的工程默认方案。

#### 图 7 Sport 主结果图

![Sport主结果图](results/sport/seed_42/plots/tracking_energy_sport.png)

#### 图 8 Sport 牵引/回收分段图

![Sport牵引回收分段图](results/sport/seed_42/plots/diagnostic_traction_regen_sport.png)

#### 图 9 Sport 工作点分布图

![Sport工作点分布图](results/sport/seed_42/plots/diagnostic_operating_points_sport.png)

---

## 6. 结果总体评价

综合来看，当前系统已经达到以下几个比较关键的阶段性成果：

1. 已成功训练出多风格节能控制器  
不仅能输出一个策略，而且能输出三类不同驾驶取向的策略。

2. 已经具备真实节能能力  
不是单纯在训练口径上好看，而是在真实电池能耗上得到正向收益。

3. 已经具备较好的可解释性  
通过主结果图、节能效果图、扭矩偏差图、工作点分布图等，可以清楚展示节能是如何产生的。

4. 已经初步具备比赛展示价值  
`eco` 适合展示稳健性，`normal` 适合展示均衡性，`sport` 适合展示性能上限。

---

## 7. 当前不足与后续方向

虽然当前结果整体较好，但仍存在两个需要诚实说明的方面：

### 7.1 Normal 模式的压力测试泛化仍可提升

`normal` 在标准路况中表现良好，但在 `stress` 中仍未完全满足跟踪约束，说明在复杂工况下的泛化仍有优化空间。

### 7.2 高节能与高鲁棒性的统一仍是重点

`sport` 展现了高节能上限，`eco` 展现了稳健性，但要进一步实现“既高节能又高鲁棒”的统一，仍需要在训练道路多样性、状态设计和泛化训练策略上继续优化。

---

## 8. 可用于比赛答辩的结论表述

本项目提出了一种基于参考轨迹和强化学习残差控制的电动车节能优化方法。该方法通过将节能目标、轨迹跟踪和平顺性约束统一纳入强化学习框架，并结合拉格朗日约束调节机制，实现了多风格节能控制器的训练与评估。实验结果表明：

- `eco` 模式在标准与压力工况下均实现稳定正节能，适合作为稳健基线；
- `normal` 模式在标准工况下实现 `3.24%` 的真实节能，体现出良好的节能与跟踪平衡能力；
- `sport` 模式在收紧跟踪约束后，实现了 `26.16%` 的标准工况节能和 `31.60%` 的压力工况节能，展现出较高的性能上限。

整体来看，本方案已经证明：在统一道路与参考驾驶条件下，强化学习能够学习到具有可解释性、可风格化和可展示性的电动车节能控制策略。

---

  